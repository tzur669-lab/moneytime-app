{
  "name": "moneytime-app",
  "version": "1.0.0",
  "description": "מעקב שעות Pro - Native Android App",
  "scripts": {
    "build": "echo 'Web assets ready'",
    "cap:sync": "npx cap sync android",
    "cap:build": "npx cap build android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
